name: Deploy on Target Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker compose build

#      - name: Restart app
#        run: |
#          docker compose down
#          docker compose up -d
